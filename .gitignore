# Foundry
cache/
out/
broadcast/
!broadcast/*/*/run-latest.json
docs/book/

# Coverage
lcov.info
coverage/
*.lcov

# Environment
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
.vscode/
.idea/
*.swp

# Per-machine agent settings. `.claude/launch.json` is shared and committed.
.claude/settings.local.json

# Node (future frontend)
node_modules/

# Deployment artifacts are per-chain and regenerated by the scripts.
deployments/*.json
